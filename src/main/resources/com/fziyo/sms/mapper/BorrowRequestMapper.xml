<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fziyo.sms.mapper.BorrowRequestMapper">
    <insert id="insert">
        insert into borrow_request(asset_id, borrow_id, start_date, end_date, status,
        approver_id, approve_time) values(#{assetId}, #{borrowId}, #{startDate}, #{endDate}, #{status})
    </insert>
    <delete id="deleteByIds">
        delete from team where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <update id="update">
        update borrow_request
        <set>
            <if test="startTime != null"> start_time = #{startTime},</if>
            <if test="endTime != null"> end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="approverId != null">approver_id = #{approverId},</if>
            <if test="approveTime != null">approve_time = #{approveTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>